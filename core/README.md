# core

파이프라인 전반에서 공통으로 사용하는 유틸리티 모음입니다.

**파일**
- `class_names.py`: FFT-75 클래스 목록 및 카테고리 매핑
- `dataset.py`: `.npz` 데이터 로딩 유틸
- `feature_extractor.py`: 바이트/통계/바이그램/매직바이트 특징 추출
- `paths.py`: 경로 설정과 로컬 오버라이드 지원
